# config.py

import os
from dotenv import load_dotenv

load_dotenv() # Esto carga las variables del archivo .env

OPENWEATHER_API_KEY = os.getenv("OPENWEATHER_API_KEY")
NEWSAPI_API_KEY = os.getenv("NEWSAPI_API_KEY")

# Puedes añadir otras configuraciones aquí, ej. ciudad por defecto, país por defecto, etc.
DEFAULT_CITY = "La Serena" # Cambiado a La Serena
DEFAULT_COUNTRY = "cl" # Código de país ISO 3166-1 alpha-2 para Chile
